language: python

python:
  - "2.6"
  - "2.7"
  - "3.3"
  - "3.4"
  - "3.5"
  - "3.6"
  - "pypy"

install:
  - pip install . --quiet
  - pip install nose --quiet
  - pip install speaklater --quiet
  - pip install flake8 --quiet

before_script:
  - flake8 .

script:
  - nosetests

branches:
  only:
    - master
